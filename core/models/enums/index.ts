export enum STORAGE_KEYS {
    LANGUAGE = "language",
    CATEGORY = "category",
    SCROLL_POSITION = "scroll-position",
    BLOG_PAGE = "blog-page",
    BLOG_QUERY = "blog-query",
    IS_LOGIN = "is_login",
    IS_ACCEPT_COOKIES = "is_accept_cookies"
}

export enum MESSAGES_TYPES {
    ERROR = "error",
    INFO = "info"
}

export enum SERVER_STATUSES {
    UNAUTHORIZED = 401,
    FORBIDDEN = 403
}

export enum ROUTES {
    MAIN = "/",
    ADMIN_AUTH = "/admin/auth",
    ADMIN_EDITOR = "/admin/editor",
    WORK = "/portfolio",
    SERVICES = "/services",
    COMPANY = "/company",
    BLOG = "/blog",
    CONTACT = "/partner",
    OUR_PROCESS = "/our-process",
    PRIVACY = "/privacy-policy",
    TERMS = "/terms",
    FAQ = "/faq",
    TESTIMONIES = "/testimonies",
    OCR = "/dibba-chatbot"
}

export enum LANGUAGES {
    EN = "en"
}

export enum LIMITS {
    BLOG = 7
}